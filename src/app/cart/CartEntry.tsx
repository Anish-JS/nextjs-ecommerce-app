"use client";

import { CartItemWithProduct } from "../lib/db/cart";

interface CardEntryProps{
    cartItem:CartItemWithProduct
}

const CartEntry = ({cartItem}:CardEntryProps) => {
    return (  );
}
 
export default CartEntry;